package assortment_of_things;


import com.fs.starfarer.api.Global;
import com.fs.starfarer.api.campaign.CampaignEventListener;
import com.fs.starfarer.api.campaign.CampaignFleetAPI;
import com.fs.starfarer.api.campaign.SectorAPI;
import com.fs.starfarer.api.campaign.listeners.FleetEventListener;
import org.apache.log4j.Level;
import org.apache.log4j.Logger;

public class Runcodes  {

    public void Example() {


/*        import com.fs.starfarer.api.Global;
import com.fs.starfarer.api.campaign.CampaignEventListener;
import com.fs.starfarer.api.campaign.CampaignFleetAPI;
import com.fs.starfarer.api.campaign.SectorAPI;
import com.fs.starfarer.api.campaign.listeners.FleetEventListener;
import org.apache.log4j.Level;
import org.apache.log4j.Logger;

        Logger log = Global.getLogger(SectorAPI.class);
        log.setLevel(Level.ALL);


        log.debug("");
        log.debug("CampaignEventListeners");
        for (CampaignEventListener listener : Global.getSector().getAllListeners()) {
            log.debug("Event Listener: " + listener.getClass().getName());
        }

        for (CampaignFleetAPI fleet : Global.getSector().getPlayerFleet().getContainingLocation().getFleets()) {

            log.debug("");
            log.debug("Fleet: " + fleet.getName());

            for (FleetEventListener listener : fleet.getEventListeners()) {
                log.debug("Fleet Listener: " + listener.getClass().getName());
            }

        }*/

    }

}
